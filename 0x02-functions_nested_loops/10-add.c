#include "main.h"

/**
 * add - adds two integers and returns the result
 * @k: int to be added to l
 * @l: int to be added to k
 * Return: sum of k and l
 */

int add(int k, int l)
{
int m;
m = k + l;
return (m);
}
