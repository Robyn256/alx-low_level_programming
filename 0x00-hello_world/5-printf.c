#include <stdio.h>
/**
 * main - A progam that prints a line using the printf function
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome ia a piece of art,\n")
return (0);
}
